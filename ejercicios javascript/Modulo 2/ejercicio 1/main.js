// Modulo 2 - ejercicio 1

var card = '4c';
if (card[1] ==='c' && card[0] === '4') {
    console.log('Es un cuatro de corazones');
}

/* Al probar el programa con otra carta devuelve 'undefined'
porque no hemos declarado que tiene que devolver si es un 'else'*/
